generator client {
  provider        = "prisma-client"
  output          = "../../generated/prisma"
  engineType      = "client"
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
}
