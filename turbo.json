{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [".env", ".env.example", ".nvmrc", "pnpm-workspace.yaml"],
  "globalEnv": ["NODE_ENV", "CI"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "paraglide:compile"],
      "outputs": ["dist/**", ".svelte-kit/**", "build/**", "generated/**"]
    },
    "dev": {
      "dependsOn": ["^build", "paraglide:compile"],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["paraglide:compile"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build", "paraglide:compile"],
      "outputs": []
    },
    "paraglide:compile": {
      "inputs": ["project.inlang/**", "messages/**"],
      "outputs": ["src/lib/paraglide/**"]
    },
    "db:generate": {
      "outputs": ["generated/**"]
    }
  }
}
