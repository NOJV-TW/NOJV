apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: nojv-deploy
  namespace: flux-system
spec:
  providerRef:
    name: discord
  eventSeverity: info
  eventSources:
    - kind: HelmRelease
      name: nojv
      namespace: nojv
