apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: nojv-workspace
spec:
  template:
    spec:
      containers:
        - image: REGION-docker.pkg.dev/PROJECT_ID/REPOSITORY/workspace:latest
          ports:
            - containerPort: 4173
