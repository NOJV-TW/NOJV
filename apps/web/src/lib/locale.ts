/**
 * Default locale used for database content (problem statements, user locale, etc.).
 * This is NOT the Paraglide baseLocale — it's the application-level default.
 */
export const DEFAULT_LOCALE = "zh-TW";
